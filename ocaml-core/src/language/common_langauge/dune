(include_subdirs unqualified)

(library
 (name common_language)
 (flags -w -20-33-3)
 (preprocess
  (pps ppx_log))
 (libraries ppx_log stdlibExt basic basic_collection)
 (modules
  NumericValue
  Interop
  RegFileF
  NumericMemory
  ROMCombinedMemory
  FailableMemory
  ROMCombinedFailableMemory
  NumericBop
  NumericUop
  Block
  RawInst))
